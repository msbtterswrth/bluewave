<article{{ attributes }}>
    <div class="text">
        <h4><a href="{{ url }}">{{ label }}</a></h4>
        <p>{{ content.body.0 }}</p>
        <a href="{{ url }}" class="btn underline red" aria-label="{{ label.0 }}">Read More</a>
    </div>
</article>